project(systems)

file(GLOB SYSTEMS_HEADERS ${CMAKE_CURRENT_SOURCE_DIR})
file(GLOB SYSTEMS_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)

add_library(${PROJECT_NAME} STATIC ${SYSTEMS_SOURCES})

target_include_directories(${PROJECT_NAME} PUBLIC ${SYSTEMS_HEADERS})